export function runBlock ($log) {
  'ngInject';
  $log.debug('runBlock end');
 /* let FB = $window.FB;
  if (!angular.isObject(FB)) {
    return;
  }
  FB.getLoginStatus((function(response) {
    this.$log.debug(response);
  }).bind(this));*/
}
